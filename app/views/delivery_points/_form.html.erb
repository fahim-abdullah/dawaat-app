<%= form_with(model: delivery_point, local: true) do |form| %>
  <% if delivery_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery_point.errors.count, "error") %> prohibited this delivery_point from being saved:</h2>

      <ul>
      <% delivery_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions form-group">
    <%= form.submit class: 'btn btn-sm btn-primary' %>
  </div>
<% end %>
